# Normalize to LF in the repo and in the working tree. CI regenerates
# content/manifest.json and compares it against the committed copy; a CRLF
# checkout would make that comparison fail for no real reason.
* text=auto eol=lf
*.png binary
*.jpg binary
*.woff2 binary
